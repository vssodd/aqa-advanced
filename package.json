{
  "name": "qauto-dz24-network-api",
  "version": "1.0.0",
  "description": "Playwright HW 29.1: response mocking + APIRequestContext tests for /api/cars",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5"
  }
}
