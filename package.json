{
  "name": "qauto-dz23-fixtures",
  "version": "1.0.0",
  "description": "Playwright fixtures + storageState: userGaragePage fixture for qauto",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5"
  }
}
