{
  "name": "qauto-dz26-ci-docker",
  "version": "1.0.0",
  "description": "HW 31.1: GitHub Actions workflow running Cypress and Playwright tests inside Docker containers",
  "scripts": {
    "cy:run": "cypress run --browser chrome",
    "cy:run:firefox": "cypress run --browser firefox",
    "pw:test": "playwright test"
  },
  "devDependencies": {
    "cypress": "^13.6.0",
    "@playwright/test": "1.39.0"
  }
}
